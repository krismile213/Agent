# AGENT.md — 项目记忆模板

> 用法：复制本文件为 `AGENT.md` 放到**被管理的工作目录根部**（如 `work-main\`），
> `mini_agent.py --cwd <该目录>` 启动时自动注入系统提示。这个文件就是你教给 agent
> 的业务常识——口径、红线、默认习惯。示例内容以 work-main 为例。

## 工作区主题

新品售前流程（手机膜/手机壳）数据分析：钉钉审批数据 → 时效分析 → 流程复盘。

## 关键口径（不可违反）

- 全流程红线：Part1 取得手机数据 → Part8 最后到货 **≤60 自然日**。
- Part1~Part9 共 9 个钉钉审批子流程；「审批记录」格式：`操作人|动作|时间|备注`，节点以 `;` 分隔。
- **18 位审批编号必须按文本处理**，任何情况下不得转为数字（Excel 科学计数法会丢精度）。
- 路线复盘有两套口径：`action`（动作映射）/ `system`（系统口径），**结论不可混用**，引用时必须注明口径。
- 数据目录：`data/手机膜/手机膜` 为自动同步目录（每天 09:00/14:30 覆盖式刷新）；`手机膜8.28` 等带批次名的目录是手工快照。

## 默认习惯

- 默认批次：手机膜 8.28。
- 分析产出落到各项目的 `output/` 目录，不要写到数据目录里。
- 中文回答；业务数字必须注明来自哪个文件/哪次执行。

## 常用命令

```
python 手机膜时效分析/src/analysis.py                 # 时效分析(默认8.28批次)
python 流程复盘/route_check.py --sku G531            # 路线复盘(动作口径)
python 流程复盘/route_check_sys.py --sku G531        # 路线复盘(系统口径)
python 亚马逊前台排查/front_check.py G570            # 18站前台排查
```
